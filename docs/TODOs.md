# TODOs

## Command Schema

- Revisit `openapi generate-all`: this command is currently a shortcut for `openapi generate-docs generate-all`, but the name can be confused with the full OpenAPI generation pipeline. Consider removing it, renaming it, or exposing only `openapi generate-docs generate-all` in the public CLI.
