# AI Generation Tool Package

This package owns the Ballerina tool metadata for `bal aigen`.

Build the Java CLI artifact first, then install this tool package:

```bash
cd ../
./gradlew :aigen-cli:build
cd aigen-tool
bal tool install
```

